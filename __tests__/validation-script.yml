input-b:
  type: string
  regex: 'foo\.bar.*' # must be ecma scripts regex
milliseconds:
  type: number
time-to-live:
  type: number
  less-than: 1000
  greater-than: 0
enabled:
  type: boolean
working-directory:
  type: string
  length: 10
unknown:
  required: false # should not appear in the report if not valid
  type: number
