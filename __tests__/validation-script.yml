input-b:
  type: string
  regex: 'foo\.bar.*' # must be ecma scripts regex
  length: 90
milliseconds:
  type: number
time-to-live:
  type: number
  less-than: 1000
  greater-than: 0
enabled:
  type: boolean
  nullable: true
working-directory:
  type: string
  length: 10
unknown:
  type: number
