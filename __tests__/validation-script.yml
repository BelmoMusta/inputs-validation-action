input-b:
  must-be: regex
  value: 'foo\.bar.*' # must be ecma scripts regex
milliseconds:
  must-be: number
time-to-live:
  must-be: number
  less-than: 1000
  greater-than: 0
enabled:
  must-be: boolean
unknown:
  required: false # should not appear in the report if not valid
  must-be: number

